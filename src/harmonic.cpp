#include "harmonic.h"

Harmonic::Harmonic(const Configuration& cfg, std::ostream& info, std::ostream& data, std::ostream& warn) : 
	cfg(cfg),
	info(info),
	data(data),
	warn(warn) {
}

void Harmonic::run() {
}